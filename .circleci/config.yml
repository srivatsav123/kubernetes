version: 2
jobs:
  build:
    machine: true
    docker:
    - image: circleci/golang:1.11.1
    working_directory: /go/src/k8s.io/kubernetes
    steps:
    - checkout
    - run: go build ./cmd/kubectl
  test:
    docker:
    - image: circleci/golang:1.11.1
    working_directory: /go/src/k8s.io/kubernetes
    steps:
    - checkout
    - run: go test ./pkg/controller
  deploy-job:
        steps:
          - add_ssh_keys:
              fingerprints:
                - "17:48:d1:31:ea:1f:65:31:2f:3b:49:e3:f0:35:7a:e1"  
workflows:
  version: 2
  workflow:
    jobs:
    - build
    - test:
           requires:
             - build
    - deploy-job:
           requires:
             - test
        
    
